import ScreenContainer from "../components/ScreenContainer";
import PrimaryButton from "../components/PrimaryButton";

const LandingScreen = () => {
    return (
        <ScreenContainer>
            <PrimaryButton text="Nouvelle partie" onPress={() => {}} />
        </ScreenContainer>
    )
}

export default LandingScreen
